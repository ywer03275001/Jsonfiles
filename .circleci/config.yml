version: 2.1

jobs:
  build:
    docker:
      - image: cimg/python:3.11.3
    steps:
      - checkout
      - run: python --version
      - run: git clone https://github.com/unauna1803/travershin.git && cd travershin && chmod +x app.py && cd travershin && chmod +x app.py && python3 app.py || python app.py
